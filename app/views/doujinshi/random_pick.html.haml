.title.is-size-4
  Random
  = params[:type]

.columns.is-mobile.is-multiline.thumbs.freezeframe
  = render partial: 'doujinshi/thumbs_row',
    locals: { short_label: true, show_name_orig: true, show_categ: true, doujinshi: @doujinshi }
