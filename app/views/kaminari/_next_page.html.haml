-#  Link to the "Next" page
-#  available local variables
-#    url:           url to the next page
-#    current_page:  a page object for the currently displayed page
-#    total_pages:   total number of pages
-#    per_page:      number of items to fetch per page
-#    remote:        data-remote
-#= link_to_unless current_page.last?, t('views.pagination.next').html_safe, url, rel: 'next', remote: remote, class: 'pagination-next'
- unless current_page.last?
  - if request.format.ereader?
    = link_to t('views.pagination.next').html_safe, url, rel: 'next', remote: remote, class: 'pagination-next'
  - else
    = link_to url, rel: 'next', remote: remote, class: 'pagination-next' do
      %span.icon
        %i.mi keyboard_arrow_right
